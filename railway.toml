[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --chdir inventory_app -w 2 --timeout 60 -b 0.0.0.0:$PORT app:app"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
